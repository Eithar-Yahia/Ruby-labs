
<%= form_with(model: [ @article, @article.comments.build ], local: true) do |form| %>
  <p>      
    <%= current_user.email %>
  </p>
  
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>